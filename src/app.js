// This is a lightweight JavaScript file added for proper GitHub Linguist technology detection
document.addEventListener('DOMContentLoaded', () => {
    const display = document.getElementById('temperature-display');
    display.textContent = 'Loading temperature data...';
});

// Example JavaScript file for GitHub Linguist detection
console.log("Hello from JavaScript!");

// Example function
function exampleFunction(param) {
    return param * 42;
}

// Example usage
const result = exampleFunction(2);
console.log(`Result: ${result}`); 